CSS:
	-gameOptions,Details,Map relative to parent div

API:
	validate get/post variables 

	map.php:
	{
		add isset($_GET['x/y'])
		show shadow tile
	}

	resources.php
	{
		add init resources and resources income for new players
	}

JS:
	Store conquered tile (to show it details without refreshing board)
	getRegion instead of Tile (getTile(x,y) => getRegion(xFrom,yFrom,xTo,yTo))
	DONE: class to store tiles (storeTile method)
	DONE: getTile - fix to async.


PHP:
	DB-interface to manage opeations on resources etc.
